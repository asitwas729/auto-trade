# 한국투자증권 자동매매 시스템 의존성

# HTTP / WebSocket
requests>=2.31.0
websocket-client>=1.7.0
aiohttp>=3.9.0

# 환경변수
python-dotenv>=1.0.0

# 데이터 처리
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0         # Parquet 저장

# 기술지표 (선택 - 직접 구현으로 대체 가능)
# ta-lib은 Windows에서 별도 설치 필요: pip install TA-Lib
# ta>=0.11.0

# 강화학습
stable-baselines3>=2.2.0
gymnasium>=0.29.0
torch>=2.1.0             # stable-baselines3 의존성

# 알림 (Slack은 requests로 처리)
# discord.py>=2.3.0       # Discord 사용 시

# 백테스트 시각화 (선택)
matplotlib>=3.8.0
# plotly>=5.18.0

# 테스트
pytest>=7.4.0
pytest-asyncio>=0.23.0
